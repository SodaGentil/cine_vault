```
typedef struct BiTNode{

    ElemType data;

    struct BiTNode *lchild,*rchild;

}BiTNode,*BiTree;
```
- **最前面的 `BiTNode`**：是给编译器看的**真名**，为了在花括号里面能实现“自己指向自己”（自引用）
    
- **最后面的 `BiTNode` 和 `*BiTree`**：是给程序员看的**外号**，为了出了花括号之后，写代码能少敲几个键盘字母

