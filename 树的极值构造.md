### 1. $n$ 个结点的 $m$ 叉树的（最大/最小）高度

这主要考察你对“树的度”和“m叉树”定义的理解。

- **最大高度（最瘦）**：
    
    - **思路**：让它退化成一条单链表，每一层只有一个结点。
        
    - **结论**：最大高度 $h_{max} = n$。
        
- **最小高度（最胖）**：
    
    - **思路**：让它变成一棵“满 $m$ 叉树”，每一层都塞满 $m$ 个孩子，直到塞不下为止。
        
    - **结论**：根据等比数列求和推导，最小高度为：
        
        $$h_{min} = \lceil \log_m(n(m-1) + 1) \rceil$$

### 2. $n$ 个结点的二叉树，（最多/最少）有多少个叶子结点？

- **最少叶子结点（最瘦）**：
    
    - **思路**：退化成一根棍子（单链表），每个结点都只有一个孩子（无论左右）。
        
    - **结论**：最少只有 **1** 个叶子结点。
        
- **最多叶子结点（最胖）**：
    
    - **思路**：尽可能早地让结点分叉。其实就是把它构造成一棵**完全二叉树**。
        
    - **结论**：在完全二叉树中，度为 1 的结点 $n_1$ 要么是 0，要么是 1。配合公式 $n_0 = n_2 + 1$ 和 $n = n_0 + n_1 + n_2$，可以推导出最多叶子结点数为：
        
        $$n_0 = \lceil \frac{n}{2} \rceil$$
### 3.求结点数

- **求“最少”结点（往“瘦”了画）：** 目标是让树**尽可能地退化成单链表（长条形）**。在满足题目硬性规定的前提下，每一层放的结点越少越好。
    
- **求“最多”结点（往“胖”了画）：** 目标是让树**尽可能地变成满二叉树或完全二叉树（金字塔形）**。每一层都尽可能把坑位塞满。